export enum CollectionStatus {
  active = "active",
  draft = "draft",
  archived = "archived"
}

export enum ProductStatus {
  active = "active",
  inactive = "inactive",
  lowStock = "lowStock",
  outOfStock = "outOfStock"
}

export enum ProductUnit {
  kg = "kg",
  piece = "piece",
  other = "other"
}